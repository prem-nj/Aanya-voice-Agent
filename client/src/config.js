// Central runtime configuration for the app.
// Kept in its own module so the voice-assistant logic and the API layer can
// reference it without creating a circular import with App.jsx.
export const ServerUrl = "https://aanya-voice-agent.onrender.com";
export const CLIENT_URL = "https://aanya-voice-agent-1.onrender.com";